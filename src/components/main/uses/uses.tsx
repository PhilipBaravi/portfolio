import { FC } from "react";
import UsesCards from "./uses-cards";

const Uses: FC = () => {
  return (
    <section className="w-[95%] md:w-[80%] lg:w-[75%] mx-auto">
      <UsesCards />
    </section>
  );
};

export default Uses;
